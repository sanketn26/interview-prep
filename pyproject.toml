[project]
name = "senior-engineer-academy"
version = "0.1.0"
description = "Interactive Senior Engineer Academy — examples and site tooling"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests/python"]
pythonpath = ["."]
addopts = "-q"
